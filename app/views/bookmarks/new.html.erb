<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.where.not(id: @list.bookmarks.pluck(:movie_id)) %>
  <%= f.submit %>
<% end %>
